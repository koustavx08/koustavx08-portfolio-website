# Resend API key for the contact form (https://resend.com/api-keys)
RESEND_API_KEY=

# Optional: verified sender address (falls back to Resend's shared test
# sender if unset, which can only deliver to the account owner's own inbox).
CONTACT_FROM_EMAIL="Portfolio Contact Form <contact@koustavx08.dpdns.org>"
